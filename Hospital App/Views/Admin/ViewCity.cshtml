@model IEnumerable<City>
@{
    ViewData["Title"] = "List Of Cities";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="row">
    <ol class="breadcrumb">
        <li>
            <a href="#">
                <em class="fa fa-home"></em>
            </a>
        </li>
        <li class="active">Cities</li>
        <li class="active">List Of Cities</li>
    </ol>
</div>
<div class="container px-6 mx-auto grid">

<h2><b>@ViewData["Title"]</b></h2>

<p>
    <a asp-action="AddCity" asp-controller="Admin" class="btn btn-primary">Add New City</a>
</p>

<table class="table table-hover">
    <thead>
        <tr>
            <th>City Name</th>
            <th>City Abbriviation</th>
            <th>Action</th>


        </tr>
    </thead>
    <tbody>
        @foreach (var nurse in Model)
        {
            <tr>
                <td>@nurse.CityName</td>
                <td>@nurse.CityAbbreviation</td>

                <td>
                    <a asp-action="EditAppointment" asp-action="UpdateNurse" asp-route-id="@nurse.CityID">
                        <i class="fa fa-pencil-square-o" style="font-size: 19px"></i>
                    </a> |
                    <a asp-action="DetailOfAppointment" asp-action="UpdateNurse" asp-route-id="@nurse.CityID">
                        <i class="fa fa-info-circle" style="font-size: 19px; color: lightblue"></i>
                    </a> |
                    <a asp-action="DeleteAppointment" asp-action="UpdateNurse" asp-route-id="@nurse.CityID">
                        <i class="fa fa-trash" style="font-size: 19px; color: red"></i>
                    </a>
                </td>

            </tr>
        }
    </tbody>
</table>
</div>